Python Practice

                                  #1. Guessing Game
                                  
import random
random_number = random.randint(1,10)

Guess = 0
while Guess != random_number:
    Guess = int(input('Guess a number between one and 10: '))
 
    if (Guess > random_number):
       print('Guess lower')

    if (Guess < random_number):
       print('Guess higher')

else:
    print("yay you win!")
    
    
                                 #2. Car Game
                                 
command = "" #set command varriable to empty string
started = False

while True:
    command = input("> ").lower() #whatever the user enters, store it in command varriable
    if command == "start": #comapre user input to see if it's 'start'
        if started:
            print('Car is already started!')
        else:
            started = True
            print('Car is started...')
    elif command == "stop":
        if not started:
            print('Car is already stopped!')
        else:
            started = False
            print("Car is stopped.")

    elif command == "help":
        print("""
        start - to start the car
        stop - to stop the car
        quit - to quit""")
    elif command == "quit":
        break
    else:
        print("sorry, I don't understand that") #run if the use doesn't enter any of the above  



                                                       #3. practice For loop iteration (Love a little pop culture reference)
                                                           
                                                           
                                                           
names = ["Kelly", "Michelle", "Beyonce"] #make a list and asgin a variable

for n in names: #for every item or 'n' in that list called names do something
    print(n+",can you handle this?") #something= print that item (n) and add "can you handle it" to it
print("I don't think they can handle this!") #when you're done with each item print the quote



                                                        #4. practice For loop repetion
                                                        
for items in range(3): #each pass through the loop adds to # of "items" which will equal 0,1,2 and stop at three
    print("Hello, hello, hello, how low")
print("Hello, hello hello") 
                                                        
                                                         #5. Nested loop to print an F shape
                                                         
numbers = [5,2,5,2,2]

for x_count in numbers: #
    output = ''
    for count in range(x_count):
        output += 'x'
    print(output)                                                         
                                                       #6. Using lists to find the biggest number
                                                       
                                                       
                                                       numbers = [3,6,3,8,4] #define numbers to look through
max = numbers[0] #temporarily set max to the first number
for number in numbers:
    if number > max: #compare each item in mubers to max over each iteration 
        max = number #if an item is greater than max it becomes max
print(max)


#7 Text emoji converter
                                                         
message = input(">")
words = message.split(' ') #.split turns string into seperate parts wherever theres a space
emojis = { #create a dictionary to create a key value pair w/ emoji and smiley
    ":)": "😊",
    ":(": "😢"
}
output = ""
for word in words:
    output += emojis.get(word, word) + " "
print(output)





                                                         #8 Practice def. functions
def greet_users():
    print('Hi there!')
    print('Welcome home')


print("start")
greet_users()
print("finish")
